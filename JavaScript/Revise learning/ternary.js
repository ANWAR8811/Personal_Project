// var age = 18;

// var type;

// if (age >= 18) {
//   type = "adult";
// } else {
//   type = "child";
// }

// console.log(type, age);

// var type = age >= 18 ? "adult" : "child";
// console.log(type, age);

// nested condition below

// var age = 1;
// var type = age >= 18 ? "adult" : age < 10 ? "child" : "young";
// console.log(type, age);

//true false shortcut
var a = 6;

var access = a < 10;
console.log(access);
